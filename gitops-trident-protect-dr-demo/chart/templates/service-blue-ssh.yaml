apiVersion: v1
kind: Service
metadata:
  name: centos-vm-blue-ssh
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: ssh
      port: 22
      targetPort: 22
      protocol: TCP
  selector:
    kubevirt.io/domain: {{ .Values.blue.name }}
  type: ClusterIP
